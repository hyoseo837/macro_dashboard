# Database URL (local dev assumes DB in docker at 5433)
# DATABASE_URL=postgresql+psycopg://macro:macro@localhost:5433/macro_dashboard

# JWT Secret
SECRET_KEY=CHANGE_ME_random_secret

# AI - Google Gemini API Key
GEMINI_API_KEY=

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com
