# v4 Plan — News Widgets (In Progress)

Two-phase approach: Phase A (headlines) ships first. Phase B (AI) adds Gemini.

## Design Decisions

1. **Two phases**: Headlines-only (RSS) first, AI features second.
2. **Single-feed widgets**: Each news widget = one RSS feed. 1:1 like asset widgets.
3. **Feed catalog**: Predefined in code (22 feeds). Not user-editable.
4. **On-demand activation**: Feeds only fetched when a widget references them.
5. **Hourly refresh**: Articles older than 7 days cleaned up daily.
6. **Minimum size**: 2x1. Article count scales with size automatically.
7. **Click to read**: Headlines open original article in new tab.
8. **Label**: Auto-generated from feed name, user-editable.
9. **No custom RSS URLs**: Catalog only.

## Feed Catalog

22 feeds across 7 sources:

| Source | Feeds |
|--------|-------|
| BBC | world, tech, business, science, sports, entertainment |
| CNN | world, tech, business, entertainment, US |
| Reuters | world, tech, business |
| NYT | world, tech, business, science, US |
| Hacker News | technology |
| Korea Herald | global (KR) |
| CBC | global (CA) |

## Database

- `news_feeds`: id, feed_key (unique), source_name, topic, country, feed_url, last_fetched_at
- `news_articles`: id, feed_id (FK), title, url (unique), source_name, published_at, fetched_at
- `widgets.type` enum: added `"news"`

## Widget Config

```json
{ "feed_id": "bbc_tech", "label": "BBC Technology" }
```

## Implementation Phases

1. Backend: Models + feed catalog + RSS service ✅
2. Backend: API + scheduler integration ✅
3. Frontend: News widget component ✅
4. Frontend: Add/edit news widget ✅
5. Polish + version bump + docs (pending)

## Phase B — AI-Powered News (Gemini) — Deferred

- Cross-source topic feeds ("All Tech News") with AI dedup
- Overall feed with event clustering
- AI summaries (2-3 sentences per article)
- Gemini Flash via Google AI Studio (free tier: 15 RPM, 1M tokens/day)
- Widget config: `source` and `topic` become optional (null = all)
- Deps: `google-generativeai`, `GEMINI_API_KEY` env var
- Scope to be defined after Phase A is stable.
